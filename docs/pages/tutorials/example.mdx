---
layout: default
title: "Spring MVC Example 實戰指南"
nav_order: 2
parent: "教學指南"
---

# Spring MVC Example - 用戶管理系統實戰指南

## 🎯 教學目標

本教學將帶您深入了解如何基於 Spring MVC Template 構建一個完整的用戶管理系統，展示企業級 Spring Boot 應用的最佳實踐。

**學習時間**: 約 1 小時  
**適合對象**: 希望理解完整業務實現的開發者

## 📋 學習內容

- 🏗️ 用戶管理系統深度解析
- 🔄 完整的 CRUD 操作實現
- 🔍 高級搜索和分頁功能
- 💾 快取和效能優化策略
- 🎯 統一異常處理機制
- 📊 API 文檔和測試

## 🚀 快速開始

### 前置需求
- Java 21 或以上版本
- Maven 3.6 或以上版本
- (可選) Redis 服務器，用於快取功能

### 1. 啟動專案

```bash
cd projects/spring-mvc-example
mvn spring-boot:run
```

### 2. 驗證啟動成功

- **API 健康檢查**: http://localhost:8080/api/actuator/health
- **API 文檔**: http://localhost:8080/api/swagger-ui.html  
- **H2 資料庫控制台**: http://localhost:8080/api/h2-console

## 🏗️ 架構解析

### 專案結構

```
src/main/java/com/example/
├── controller/          # REST API 控制器
│   └── UserController.java
├── service/            # 業務邏輯層
│   └── UserService.java
├── repository/         # 資料存取層
│   └── UserRepository.java
├── entity/             # JPA 實體
│   ├── BaseEntity.java
│   └── User.java
├── dto/                # 資料傳輸物件
│   ├── RestResponse.java
│   ├── UserDto.java
│   ├── CreateUserRequest.java
│   └── UpdateUserRequest.java
├── mapper/             # MapStruct 映射器
│   └── UserMapper.java
├── config/             # 組態設定
├── exception/          # 異常處理
└── aspect/             # AOP 切面
```

### 分層架構設計

#### 1. Controller 層 (控制層)
```java
@RestController
@RequestMapping("/api/users")
@RequiredArgsConstructor
@Slf4j
public class UserController {
    private final UserService userService;
    
    @PostMapping
    public ResponseEntity<RestResponse<UserDto>> createUser(
            @Valid @RequestBody CreateUserRequest request) {
        UserDto user = userService.createUser(request);
        return ResponseEntity.ok(RestResponse.success("User created successfully", user));
    }
}
```

**責任**:
- 處理 HTTP 請求和響應
- 資料驗證
- 呼叫 Service 層處理業務邏輯

#### 2. Service 層 (業務邏輯層)
```java
@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)
@Slf4j
public class UserService {
    private final UserRepository userRepository;
    private final UserMapper userMapper;
    
    @Transactional
    @CacheEvict(value = "users", allEntries = true)
    public UserDto createUser(CreateUserRequest request) {
        User user = userMapper.toEntity(request);
        User savedUser = userRepository.save(user);
        return userMapper.toDto(savedUser);
    }
}
```

**責任**:
- 實現業務邏輯
- 事務管理
- 快取控制

#### 3. Repository 層 (資料存取層)
```java
@Repository
public interface UserRepository extends JpaRepository<User, Long> {
    Optional<User> findByUsername(String username);
    
    @Query("SELECT u FROM User u WHERE " +
           "(:q IS NULL OR u.firstName LIKE %:q% OR u.lastName LIKE %:q% OR u.email LIKE %:q%)")
    Page<User> searchUsers(@Param("q") String query, Pageable pageable);
}
```

**責任**:
- 資料庫操作
- 自定義查詢實現

## 🔧 核心功能實戰

### 1. 用戶 CRUD 操作

#### 創建用戶
```bash
curl -X POST http://localhost:8080/api/users \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john_doe",
    "email": "john@example.com",
    "firstName": "John",
    "lastName": "Doe",
    "phone": "+1234567890",
    "dateOfBirth": "1990-05-15"
  }'
```

#### 查詢用戶
```bash
# 根據 ID 查詢
curl http://localhost:8080/api/users/1

# 分頁查詢
curl "http://localhost:8080/api/users?page=0&size=5&sort=createdAt,desc"
```

#### 更新用戶
```bash
curl -X PUT http://localhost:8080/api/users/1 \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Updated John",
    "lastName": "Updated Doe"
  }'
```

#### 刪除用戶
```bash
curl -X DELETE http://localhost:8080/api/users/1
```

### 2. 高級搜索功能

#### 通用搜索
```bash
curl "http://localhost:8080/api/users/search?q=john"
```

#### 條件搜索
```bash
curl "http://localhost:8080/api/users/search?firstName=John&status=ACTIVE"
```

### 3. 分頁和排序
```bash
# 按創建時間降序排列，每頁10條
curl "http://localhost:8080/api/users?page=0&size=10&sort=createdAt,desc"

# 多字段排序
curl "http://localhost:8080/api/users?page=0&size=10&sort=lastName,asc&sort=firstName,asc"
```

## 💾 快取策略

### Redis 快取配置

```java
@Configuration
@EnableCaching
public class CacheConfig {
    
    @Bean
    public RedisCacheManager cacheManager(RedisConnectionFactory connectionFactory) {
        RedisCacheConfiguration config = RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofMinutes(10))
                .serializeKeysWith(RedisSerializationContext.SerializationPair
                        .fromSerializer(new StringRedisSerializer()))
                .serializeValuesWith(RedisSerializationContext.SerializationPair
                        .fromSerializer(new GenericJackson2JsonRedisSerializer()));
                        
        return RedisCacheManager.builder(connectionFactory)
                .cacheDefaults(config)
                .build();
    }
}
```

### 快取使用示例

```java
@Service
public class UserService {
    
    @Cacheable(value = "users", key = "#id")
    public UserDto getUserById(Long id) {
        // 從資料庫查詢用戶
    }
    
    @CacheEvict(value = "users", key = "#id")
    public UserDto updateUser(Long id, UpdateUserRequest request) {
        // 更新用戶並清除快取
    }
    
    @CacheEvict(value = "users", allEntries = true)
    public UserDto createUser(CreateUserRequest request) {
        // 創建用戶並清除所有用戶快取
    }
}
```

### 快取效能測試

```bash
# 第一次查詢（從資料庫讀取）
time curl http://localhost:8080/api/users/1

# 第二次查詢（從快取讀取，速度更快）
time curl http://localhost:8080/api/users/1
```

## 🔍 資料驗證

### Bean Validation 註解

```java
public class CreateUserRequest {
    
    @NotBlank(message = "Username is required")
    @Size(min = 3, max = 50, message = "Username must be between 3 and 50 characters")
    private String username;
    
    @NotBlank(message = "Email is required")
    @Email(message = "Please provide a valid email address")
    private String email;
    
    @NotBlank(message = "First name is required")
    @Size(max = 50, message = "First name cannot exceed 50 characters")
    private String firstName;
    
    @Past(message = "Date of birth must be in the past")
    private LocalDate dateOfBirth;
    
    @Pattern(regexp = "\\+[1-9]\\d{1,14}", message = "Please provide a valid phone number")
    private String phone;
}
```

### 驗證失敗測試

```bash
# 測試無效 email
curl -X POST http://localhost:8080/api/users \
  -H "Content-Type: application/json" \
  -d '{
    "username": "test",
    "email": "invalid-email",
    "firstName": "Test"
  }'
```

**預期回應**:
```json
{
  "success": false,
  "message": "Validation failed",
  "data": {
    "email": "Please provide a valid email address"
  },
  "timestamp": "2024-01-01T10:00:00"
}
```

## 🎯 統一異常處理

### 全局異常處理器

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(ResourceNotFoundException.class)
    public ResponseEntity<RestResponse<Object>> handleResourceNotFoundException(
            ResourceNotFoundException ex) {
        
        RestResponse<Object> response = RestResponse.<Object>builder()
                .success(false)
                .message(ex.getMessage())
                .timestamp(LocalDateTime.now())
                .build();
                
        return ResponseEntity.status(HttpStatus.NOT_FOUND).body(response);
    }

    @ExceptionHandler({MethodArgumentNotValidException.class})
    public ResponseEntity<RestResponse<Object>> handleValidationException(
            MethodArgumentNotValidException ex) {
        
        Map<String, String> errors = new HashMap<>();
        ex.getBindingResult().getAllErrors().forEach(error -> {
            String fieldName = ((FieldError) error).getField();
            String errorMessage = error.getDefaultMessage();
            errors.put(fieldName, errorMessage);
        });
        
        RestResponse<Object> response = RestResponse.<Object>builder()
                .success(false)
                .message("Validation failed")
                .data(errors)
                .timestamp(LocalDateTime.now())
                .build();
                
        return ResponseEntity.status(HttpStatus.BAD_REQUEST).body(response);
    }
}
```

### 異常測試

```bash
# 查詢不存在的用戶
curl http://localhost:8080/api/users/999
```

**預期回應**:
```json
{
  "success": false,
  "message": "User not found with id: 999",
  "timestamp": "2024-01-01T10:00:00"
}
```

## 🔄 AOP 橫切關注點

### 日誌切面

```java
@Aspect
@Component
@Slf4j
public class LoggingAspect {

    @Around("@annotation(org.springframework.web.bind.annotation.RequestMapping) || " +
            "@annotation(org.springframework.web.bind.annotation.GetMapping) || " +
            "@annotation(org.springframework.web.bind.annotation.PostMapping)")
    public Object logExecutionTime(ProceedingJoinPoint joinPoint) throws Throwable {
        long startTime = System.currentTimeMillis();
        
        try {
            Object result = joinPoint.proceed();
            long endTime = System.currentTimeMillis();
            
            log.info("Method {} executed in {} ms", 
                    joinPoint.getSignature().getName(), 
                    endTime - startTime);
            
            return result;
        } catch (Exception e) {
            log.error("Exception in method {}: {}", 
                    joinPoint.getSignature().getName(), 
                    e.getMessage());
            throw e;
        }
    }
}
```

### 審計切面

```java
@Aspect
@Component
@Slf4j
public class AuditAspect {

    @AfterReturning(pointcut = "@annotation(org.springframework.web.bind.annotation.PostMapping)", 
                    returning = "result")
    public void auditCreate(JoinPoint joinPoint, Object result) {
        log.info("AUDIT: Created resource via {}", joinPoint.getSignature().getName());
    }

    @AfterReturning(pointcut = "@annotation(org.springframework.web.bind.annotation.DeleteMapping)")
    public void auditDelete(JoinPoint joinPoint) {
        log.info("AUDIT: Deleted resource via {}", joinPoint.getSignature().getName());
    }
}
```

## 📊 API 文檔與測試

### Swagger UI 使用

1. 訪問: http://localhost:8080/api/swagger-ui.html
2. 展開 `user-controller` 區塊
3. 選擇任意 API 端點
4. 點擊 "Try it out" 測試

### OpenAPI 配置

```java
@Configuration
public class OpenApiConfig {

    @Bean
    public OpenAPI customOpenAPI() {
        return new OpenAPI()
                .info(new Info()
                        .title("User Management API")
                        .version("1.0.0")
                        .description("Spring Boot 用戶管理系統 API 文檔"))
                .servers(List.of(
                        new Server().url("http://localhost:8080").description("開發環境"),
                        new Server().url("https://api.example.com").description("生產環境")));
    }
}
```

## 📈 效能監控

### Actuator 監控端點

```bash
# 健康檢查
curl http://localhost:8080/api/actuator/health

# JVM 記憶體使用
curl http://localhost:8080/api/actuator/metrics/jvm.memory.used

# HTTP 請求統計
curl http://localhost:8080/api/actuator/metrics/http.server.requests
```

### 效能優化策略

1. **資料庫索引優化**
```sql
-- 用戶名唯一索引
CREATE UNIQUE INDEX idx_user_username ON users(username);

-- 郵箱唯一索引  
CREATE UNIQUE INDEX idx_user_email ON users(email);

-- 搜索複合索引
CREATE INDEX idx_user_search ON users(first_name, last_name, email);
```

2. **分頁查詢優化**
```java
@Repository
public interface UserRepository extends JpaRepository<User, Long> {
    
    // 使用 native query 優化大量資料分頁
    @Query(value = "SELECT * FROM users WHERE status = :status ORDER BY created_at DESC",
           countQuery = "SELECT count(*) FROM users WHERE status = :status",
           nativeQuery = true)
    Page<User> findByStatusNative(@Param("status") String status, Pageable pageable);
}
```

3. **快取策略優化**
```java
// 多級快取策略
@Cacheable(value = {"users-l1", "users-l2"}, key = "#id")
public UserDto getUserById(Long id) {
    // 實現邏輯
}
```

## 🧪 測試實戰

### 整合測試

```java
@SpringBootTest
@TestMethodOrder(OrderAnnotation.class)
class UserControllerIntegrationTest {

    @Autowired
    private TestRestTemplate restTemplate;

    @Test
    @Order(1)
    void shouldCreateUser() {
        CreateUserRequest request = CreateUserRequest.builder()
                .username("testuser")
                .email("test@example.com")
                .firstName("Test")
                .lastName("User")
                .build();

        ResponseEntity<RestResponse> response = restTemplate.postForEntity(
                "/api/users", request, RestResponse.class);

        assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);
        assertThat(response.getBody().isSuccess()).isTrue();
    }
}
```

### 效能測試

```bash
# 使用 Apache Bench 進行負載測試
ab -n 1000 -c 10 http://localhost:8080/api/users

# 使用 curl 測試並發
for i in {1..100}; do
  curl -X POST http://localhost:8080/api/users \
    -H "Content-Type: application/json" \
    -d "{\"username\":\"user$i\",\"email\":\"user$i@test.com\",\"firstName\":\"User\",\"lastName\":\"$i\"}" &
done
wait
```

## 🔨 客製化開發指南

### 添加新實體的完整流程

1. **創建實體類**
```java
@Entity
@Table(name = "departments")
public class Department extends BaseEntity {
    
    @Column(unique = true, nullable = false)
    private String code;
    
    @Column(nullable = false)
    private String name;
    
    @OneToMany(mappedBy = "department", cascade = CascadeType.ALL)
    private List<User> users = new ArrayList<>();
}
```

2. **創建 Repository**
```java
@Repository
public interface DepartmentRepository extends JpaRepository<Department, Long> {
    Optional<Department> findByCode(String code);
}
```

3. **創建 DTO 和 Request 類**
```java
@Data
@Builder
public class DepartmentDto {
    private Long id;
    private String code;
    private String name;
    private LocalDateTime createdAt;
}
```

4. **創建 MapStruct Mapper**
```java
@Mapper(componentModel = "spring")
public interface DepartmentMapper {
    DepartmentDto toDto(Department department);
    Department toEntity(CreateDepartmentRequest request);
}
```

5. **實現 Service 類**
```java
@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)
public class DepartmentService {
    private final DepartmentRepository departmentRepository;
    private final DepartmentMapper departmentMapper;
    
    @Transactional
    public DepartmentDto createDepartment(CreateDepartmentRequest request) {
        Department department = departmentMapper.toEntity(request);
        Department saved = departmentRepository.save(department);
        return departmentMapper.toDto(saved);
    }
}
```

6. **創建 Controller**
```java
@RestController
@RequestMapping("/api/departments")
@RequiredArgsConstructor
public class DepartmentController {
    private final DepartmentService departmentService;
    
    @PostMapping
    public ResponseEntity<RestResponse<DepartmentDto>> createDepartment(
            @Valid @RequestBody CreateDepartmentRequest request) {
        DepartmentDto department = departmentService.createDepartment(request);
        return ResponseEntity.ok(RestResponse.success("Department created successfully", department));
    }
}
```

## 🎓 最佳實踐總結

### 1. 程式碼品質
- 遵循 Google Java Style Guide
- 使用 Lombok 減少樣板程式碼
- 統一的 import 順序和組織
- SLF4j 日誌記錄（禁用 System.out.println）

### 2. 架構設計
- 嚴格的分層架構
- 依賴注入和控制反轉
- 統一的異常處理策略
- DTO 模式避免暴露內部實體

### 3. 效能優化
- 合理使用快取策略
- 資料庫索引設計
- 分頁查詢優化
- 異步處理非關鍵業務

### 4. 安全考慮
- 輸入資料驗證
- SQL 注入防護
- 敏感資料保護
- API 存取控制

### 5. 可維護性
- 完整的單元測試
- 詳細的 API 文檔
- 結構化的錯誤處理
- 監控和日誌記錄

## 🚀 下一步學習建議

1. **深入學習 Spring Security** - 實現完整的認證和授權機制
2. **微服務架構** - 將單體應用拆分為微服務
3. **事件驅動架構** - 使用 Spring Event 或 Kafka 實現解耦
4. **容器化部署** - Docker 和 Kubernetes 實戰
5. **監控和觀測性** - Micrometer + Prometheus + Grafana

---

通過這個完整的實戰指南，您已經掌握了使用 Spring Boot 構建企業級應用的核心技能。繼續實踐並探索更高級的主題，成為優秀的 Java 後端開發者！